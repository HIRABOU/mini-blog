.main-content-body
  .contents.text-center
    新着記事一覧
    - @tweets.each do |tweet|
      .contents__main
        %ul
          %li.more_list
            = link_to "/tweets/#{tweet.id}" do
              = tweet.title

